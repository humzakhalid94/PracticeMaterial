<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="transition2Sample"
            type="com.google.android.gms.example.practicematerial.Sample" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical">


        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            style="@style/PracticeMaterial.TextAppearance.Title"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            android:background="?android:colorPrimary"
            android:elevation="@dimen/elevation_header">

            <TextView
                android:id="@+id/title"
                style="@style/PracticeMaterial.TextAppearance.Title.Inverse"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|start"
                android:text="@{transition2Sample.name}" />

        </android.support.v7.widget.Toolbar>

        <ImageView
            android:id="@+id/square_red"
            style="@style/PracticeMaterial.Icon.Big"
            android:src="@drawable/circle_24dp"
            app:colorTint="@{transition2Sample.color}" />

        <TextView
            android:id="@+id/activity_title"
            style="@style/Base.TextAppearance.AppCompat.Large"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Explode" />

        <TextView
            style="@style/PracticeMaterial.Text.Body"
            android:text="@string/medium_text" />

        <Button
            android:id="@+id/exit_button"
            style="@style/PracticeMaterial.Button"
            android:text="Exit" />


    </LinearLayout>
</layout>